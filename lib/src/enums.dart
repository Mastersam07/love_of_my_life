enum LoveLanguage {
  actsOfService,
  physicalTouch,
  gifting,
  qualityTime,
  wordsOfAffirmation,
}

enum RelationshipPhase {
  talkingStage,
  friends,
  initialMushy,
  seeFinish,
  deepInLove,
  eDonCast,
}

enum BreakfastType {
  hot,
  cold,
}

enum BreakfastStatus {
  served,
  accepted,
  rejected,
}

enum BreakfastServiceMethod {
  call,
  text,
  inPerson,
  slying,
}
