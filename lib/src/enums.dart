enum LoveLanguage {
  actsOfService,
  physicalTouch,
  gifting,
  qualityTime,
  wordsOfAffirmation,
}

enum PartnerQuality {
  physicalAssets,
  money,
  both,
}

enum RelationshipPhase {
  talkingStage,
  friends,
  initialMushy,
  seeFinish,
  deepInLove,
  eDonCast,
}

enum BreakfastType {
  hot,
  cold,
}

enum BreakfastStatus {
  cooking,
  served,
  accepted,
  rejected,
}

enum BreakfastServiceMethod {
  call,
  text,
  inPerson,
  slying,
}
